apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: APP_NAME
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]

  generators:
  - list:
      elements:
      - server: https://production-control-plane:6443
        environment: production
      - server: https://staging-control-plane:6443
        environment: staging

  template:
    metadata:
      name: 'APP_NAME-{{.environment}}'
      namespace: argocd
      labels:
        app: APP_NAME
        environment: '{{.environment}}'
      annotations:
        argocd.argoproj.io/manifest-generate-paths: '.;..'
      finalizers:
        - resources-finalizer.argocd.argoproj.io

    spec:
      project: default

      source:
        repoURL: https://gitlab.com/mathod-io/infrastructure/services/argocd.git # ! change this accordingly
        targetRevision: main
        path: charts/APP_NAME   # ! change this accordingly

        helm:
          releaseName: APP_NAME # ! change this accordingly

          valueFiles:
            - values.yaml
            - environments/{{.environment}}/values.yaml
          skipCrds: false

      destination:
        server: '{{.server}}'
        namespace: crossplane-system # ! change this accordingly

      syncPolicy:
        automated:
          prune: true       # Permet de supprimer les ressources qui ne sont plus dans le repo
          selfHeal: true    # Permet à Argo CD de réconcilier automatiquement l'état
          allowEmpty: false # Ne permet pas la synchronisation si aucune ressource

        syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
        #- RespectIgnoreDifferences=true

        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
      revisionHistoryLimit: 3

      ignoreDifferences:
      - group: pkg.crossplane.io
        kind: Provider
        jsonPointers:
        - /status
      - group: aws.upbound.io
        kind: ProviderConfig
        jsonPointers:
        - /status
